## What this changes

<!-- One or two sentences. What behaviour is different after this PR? -->

## Why

<!-- The problem it solves. Link an issue with "Fixes #123" if there is one. -->

## How it was tested

- [ ] `npm test` passes
- [ ] Tried against real hardware — camera model and protocol:
- [ ] UI change: still keyboard-operable, still readable without colour

<!-- If this touches the drive path (visca.js, drivekeeper.js, gamepad.js),
     say which control test covers the new behaviour. -->
